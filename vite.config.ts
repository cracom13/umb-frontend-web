2️⃣ Copia este contenido dentro
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// Reemplaza 'NOMBRE_REPO' por el nombre exacto de tu repositorio en GitHub
export default defineConfig({
  plugins: [react()],
  base: "umb-frontend-web",
});
